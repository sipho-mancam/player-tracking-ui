[default]
bootstrap.servers=10.0.0.142:9092

[consumer]
group.id=tracking_core_consumer_10

# 'auto.offset.reset=earliest' to start reading from the beginning of
# the topic if no committed offsets exist.
auto.offset.reset=earliest